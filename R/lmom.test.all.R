"lmom.test.all" <-
function(data) {
    lmom.test.exp(data)
    lmom.test.gam(data)
    lmom.test.gev(data)
    lmom.test.glo(data)
    lmom.test.gno(data)
    lmom.test.gpa(data)
    lmom.test.gum(data)
    lmom.test.kap(data)
    lmom.test.nor(data)
    lmom.test.pe3(data)
    lmom.test.wak(data)
}

